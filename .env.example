# Data source selector (Seam 1). "mock" needs no key; "zerion" is the live adapter (Phase 5).
VITE_DATA_SOURCE=mock

# Server-side ONLY. The proxy reads this; it must NEVER reach the browser bundle,
# so it is intentionally not prefixed with VITE_. Leave blank in mock mode.
ZERION_API_KEY=
